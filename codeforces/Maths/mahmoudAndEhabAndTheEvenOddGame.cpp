#include<bits/stdc++.h>
using namespace std;

#define fastread() ios_base::sync_with_stdio(false),cin.tie(0),cout.tie(0);

int main()
{
    fastread();
    int n;
    cin>>n;
    if(n%2==0) cout<<"Mahmoud";
    else cout<<"Ehab";
    return 0;
}