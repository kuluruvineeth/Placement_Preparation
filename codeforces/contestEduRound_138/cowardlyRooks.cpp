#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define fastread() ios_base::sync_with_stdio(false),cin.tie(0),cout.tie(0);

int main()
{
    ll t,n,m;
    cin>>t;
    while(t--)
    {
        cin>>n>>m;
        for(ll i=0;i<m;i++){
            ll x,y;
            cin>>x>>y;
        }
        if(n>m) cout<<"YES\n";
        else cout<<"NO\n";
    }
    return 0;
}